# Pinned to a known-good set for reproducible installs (verified together,
# tested end-to-end). Bump deliberately, not via unpinned ranges drifting
# across environments.
streamlit==1.63.0
pypdf==6.18.1
sentence-transformers==6.0.1
faiss-cpu==1.15.0
numpy==2.5.3
python-dotenv==1.2.3
groq==1.7.0
openai==3.13.0
google-genai==2.23.0
filelock==3.32.6

# Optional advanced features: FastAPI backend (api.py)
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
slowapi==0.1.10

# Optional advanced feature: OCR for scanned PDFs (ocr.py). Also requires the
# Tesseract OCR engine installed separately on the host - see ocr.py.
pytesseract==0.3.13
pypdfium2==5.13.0
Pillow==12.3.0
